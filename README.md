# Fake-Profile-Picture-Detection
This project detects AI-generated (fake) profile pictures using image forensics and a ResNet18 CNN. The model analyzes grayscale images with Laplacian filters to detect visual artifacts from GANs. It classifies images as real or fake and is evaluated using accuracy, precision, recall, F1-score, and confusion matrix.
